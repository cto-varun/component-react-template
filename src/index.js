import component from './ReactTemplate';
import { useTemplateContext } from './context';
import { schema, ui } from './reactTemplate.schema';

export default { component, schema, ui, useTemplateContext };
